# 09 - 本次修复回归

本文件只针对本次代码修复做定向回归。每个用例对应一处 `修复项`，便于快速确认修复有效、且未引入回归。

| 修复项 | 说明 | 关联代码 |
| --- | --- | --- |
| F1 | 种植模块仅在种植时写 orbiter 位置，恢复环绕 | `PlantingModule.cs` |
| F2 | 装甲甲虫分支 `try/finally` 恢复玩家工具；缓存武器改实例 | `GrandpaFighterBehavior.cs` |
| F3 | SAN tooltip 文字可见 + 命中区域加纵向上界 | `SanBarDrawer.cs` |
| F4 | SAN 读写使用固定区域文化，去掉冗余 `Math.Max` | `SanManager.cs` |
| F5 | 砍树命中冷却随场景切换清空 | `WoodcutterExecutor.cs` |
| F6 | 修正与实现不符的注释 | `MonsterScanner.cs` / `ScythingScanner.cs` / `HighlightDrawer.cs` |

---

### RG-01 【F1】种植时爷爷环绕（P0）
→ 执行 [04-种植.md](04-种植.md) 的 **PL-02**。

### RG-02 【F2】装甲甲虫后工具恢复（P0）
→ 执行 [07-战斗.md](07-战斗.md) 的 **CB-06**。
额外：在战斗前后分别记录 `player.CurrentTool` 的名字，确保一致。

### RG-03 【F3】SAN 提示可见且不误触发（P0）
→ 执行 [08-界面与显示.md](08-界面与显示.md) 的 **UI-03**。

### RG-04 【F4】SAN 持久化跨区域稳定（P2）
→ 执行 [01-召唤与SAN系统.md](01-召唤与SAN系统.md) 的 **SN-05**。
补充验证：
1. 记录当前 SAN 值。
2. 退出到标题再读档，SAN 应保持（不因解析失败被重置为满值）。

### RG-05 【F5】跨场景命中冷却清空（P0）
→ 执行 [06-砍树.md](06-砍树.md) 的 **WC-05**。

### RG-06 【F6】注释修正（无需游戏验证）
- `MonsterScanner`：删除「snapshot」不实注释。
- `ScythingScanner`：注释改为「收所有成熟作物，不按 harvestMethod 过滤」。
- `HighlightDrawer`：SourceRect 注释改为 `(194, 388, 16, 16)`。
- 代码审查确认即可。

---

## 修复总回归检查清单

> 结果图例：✅ 通过 / ❌ 失败 / ⛔ 阻塞（未测保留 ⬜）

| 编号 | 修复项 | 结果 | 实际表现 | 备注 |
| --- | --- | --- | --- | --- |
| RG-01 | F1 种植环绕 | ⬜ |  |  |
| RG-02 | F2 工具恢复 | ⬜ |  |  |
| RG-03 | F3 SAN 提示 | ⬜ |  |  |
| RG-04 | F4 SAN 持久化 | ⬜ |  |  |
| RG-05 | F5 砍树冷却 | ⬜ |  |  |
| RG-06 | F6 注释 | ⬜ |  |  |

> 建议回归顺序：`RG-01 → RG-05 → RG-02 → RG-03 → RG-04`，先在普通农场完成前三项，再进骷髅洞穴做 RG-02，最后做界面/持久化项。
